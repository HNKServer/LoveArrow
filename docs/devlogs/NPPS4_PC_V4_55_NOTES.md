# NPPS4 PC v4.55

The Python service tree is byte-identical to Android Wrapper v4.55.

- CN home front carousel: data transfer plus active scouting banners.
- CN home back side: official manga WebView banner.
- Built-in data-transfer and manga thumbnails are served for both plain paths and `.imag` cache aliases.
- No client Museum catalogue transplant, merged Museum DB/Lua, synthetic update package, or full-package overlay remains.
- CN Museum uses the native 16-entry master; legacy automatic cross-region grants are removed safely.
- Existing CN archive version and package sequence are unchanged.
